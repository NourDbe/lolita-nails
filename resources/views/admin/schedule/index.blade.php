@extends('layouts.admin') @section('content')
<div class="admin-head"><div><span class="eyebrow">SCHEDULE</span><h1>الدوام والإجازات</h1></div></div>
<div class="admin-grid-2"><div class="admin-card"><h2>ساعات العمل الأسبوعية</h2><form method="post" action="{{ route('admin.schedule.hours') }}" class="stack-form">@csrf @php($days=['الأحد','الاثنين','الثلاثاء','الأربعاء','الخميس','الجمعة','السبت']) @foreach($days as $i=>$name) @php($row=$hours[$i]??null)<div style="display:grid;grid-template-columns:1fr 1fr 1fr auto;gap:10px;align-items:end;margin-bottom:10px"><strong>{{ $name }}</strong><label>من<input type="time" name="hours[{{ $i }}][opens_at]" value="{{ $row?->opens_at ? substr($row->opens_at,0,5) : '09:00' }}"></label><label>إلى<input type="time" name="hours[{{ $i }}][closes_at]" value="{{ $row?->closes_at ? substr($row->closes_at,0,5) : '18:00' }}"></label><label class="check"><input type="checkbox" name="hours[{{ $i }}][is_closed]" value="1" @checked($row?->is_closed)> مغلق</label></div>@endforeach<button class="btn btn-primary">حفظ الدوام</button></form></div>
<div class="admin-card"><h2>أيام الإجازة</h2><form method="post" action="{{ route('admin.schedule.days-off.store') }}" class="stack-form">@csrf<label>التاريخ<input type="date" name="date" required></label><label>السبب<input name="reason" placeholder="إجازة / مناسبة / ..."></label><button class="btn btn-primary">إضافة يوم إجازة</button></form><hr style="border:0;border-top:1px solid #ead8d5;margin:25px 0">@forelse($daysOff as $day)<div style="display:flex;justify-content:space-between;align-items:center;padding:10px 0;border-bottom:1px solid #eee"><span><b>{{ $day->date->format('Y-m-d') }}</b><br><small>{{ $day->reason }}</small></span><form method="post" action="{{ route('admin.schedule.days-off.destroy',$day) }}">@csrf @method('DELETE')<button class="link-danger">حذف</button></form></div>@empty<p>لا توجد إجازات مضافة.</p>@endforelse</div></div>
@endsection
