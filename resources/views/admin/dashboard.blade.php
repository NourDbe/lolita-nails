@extends('layouts.admin')
@section('content')
<div class="admin-head"><div><span class="eyebrow">OVERVIEW</span><h1>لوحة التحكم</h1></div></div>
<div class="stat-grid dashboard-stats"><div class="stat"><span>طلبات معلقة</span><strong>{{ $pending }}</strong></div><div class="stat"><span>مواعيد مؤكدة</span><strong>{{ $confirmed }}</strong></div><div class="stat"><span>حجوزات هذا الأسبوع</span><strong>{{ $weekCount }}</strong></div><div class="stat"><span>الإيراد المتوقع هذا الأسبوع</span><strong>{{ number_format($expectedRevenue) }} ل.س</strong></div><div class="stat"><span>الخدمة الأكثر طلباً</span><strong>{{ $topService ?: '—' }}</strong></div><div class="stat"><span>خدمات فعالة</span><strong>{{ $services }}</strong></div></div>
<div class="admin-dashboard-grid"><div class="admin-card"><div class="admin-card-head"><h2>مواعيد اليوم</h2><a href="{{ route('admin.appointments.index') }}">عرض الكل</a></div>@forelse($today as $a)<div class="appointment-row"><div><strong>{{ substr($a->appointment_time,0,5) }} — {{ $a->client_name }}</strong><small>{{ $a->service->name_ar }} • {{ $a->phone }}</small></div><span class="status {{ $a->status }}">{{ $a->status }}</span></div>@empty<p class="empty">لا توجد مواعيد اليوم.</p>@endforelse</div><div class="admin-card"><div class="admin-card-head"><h2>العميلات المتكررات</h2></div>@forelse($repeatClients as $client)<div class="appointment-row"><div><strong>{{ $client->client_name }}</strong><small>{{ $client->phone }}</small></div><span class="repeat-badge">{{ $client->total }} حجوزات</span></div>@empty<p class="empty">ستظهر هنا العميلات اللواتي حجزن أكثر من مرة.</p>@endforelse</div></div>
@endsection
